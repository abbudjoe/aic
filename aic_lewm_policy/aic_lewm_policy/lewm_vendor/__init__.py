"""Vendored LEWM model definitions needed by object checkpoints.

The checkpoint objects from `/Users/joseph/le-wm` were created with top-level
module names such as `jepa` and `module`. Runtime loading adds this directory
to `sys.path` so those imports resolve without changing the original code.
"""
